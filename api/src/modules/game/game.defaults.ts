import { GameOptions } from './game.typing';

export const gameOptions: GameOptions = {
  allowCheats: false,
  maxPlayers: -1,
  blacklistedCountries: [],
  resetAlliances: false, // Keep pre-configured geopolitical alliances by default
};
